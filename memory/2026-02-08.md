# 2026-02-08

## First Boot
- Woke up fresh. Met Mukut (Mykyat Barabanov).
- Named me **Clob** (after "slob" — trust not yet earned).
- Vibe: coy, cheeky, straight to the point, no BS, not dull.
- Emoji: 🫠
- Timezone: ET

## Setup Complete
- Telegram connected (user id: 99261167, @mukuta)
- Running on **Claude Opus 4.5** via Anthropic API (costs per token — don't waste tokens!)
- **Email**: clob@elitecurrensea.com via Gmail OAuth2 (readonly + send). Tokens at gmail_tokens.json
- **Brave Search API**: Configured and working (key: BSA3Ju...)
- **Browser**: Chromium via snap (`/snap/bin/chromium`), Playwright installed with deps

## Browser Automation Testing
- Goal: Test browser capabilities by registering accounts on websites
- **IMDB/Amazon**: Blocked. 403s, timeouts, aggressive bot detection.
- **Polymarket**: Blocked. Auth flow shows error page for headless browsers.
- **OpenClaw browser tool**: Timeout connecting to snap chromium.

### Lessons Learned
- Headless browsers get detected and blocked by most sites
- API endpoints work better when available (fetched Polymarket Super Bowl odds via their API)
- Mac Mini with real Chrome + extension relay would bypass bot detection
- Don't fight losing battles — find workarounds

## Claude Code Setup ✅
- Claude Code v2.1.37 at `/usr/bin/claude`
- **OAuth completed**: Authenticated as `hello@sonderspot.com` via Claude Max Account
- **Dual-world config solved**: 
  - OpenClaw uses Max proxy (localhost:3456)
  - Claude Code uses direct API via `ANTHROPIC_BASE_URL=https://api.anthropic.com`
- Project settings at `/root/projects/waitingforamacguffin/.claude/settings.json`:
  - Model: opus, Thinking: true, dangerouslySkipPermissions: true

## DGA Fix - waitingforamacguffin ✅
- **Task**: Add Paul Thomas Anderson as 2026 DGA winner
- **Files updated**:
  - `awards_calendar.json` — winner data
  - `precursor_wins_2026.json` — DGA win entry
  - `HistoricalTable.tsx` — 2026 row
  - `PrecursorsTable.tsx` — 2026 row
- **Commits**: `a191e71` (data) + `ac2ef18` (tables)
- **Git config fixed**: VPS needed `user.email = hello@sonderspot.com` to match Vercel
- Used `git rebase --exec` to amend commits with correct author, then force pushed
- **Vercel deployment triggered successfully**

## Blog Publishing - waitingformacguffin.com

**To publish a new blog post:**

1. **Create markdown file** in `/root/projects/waitingforamacguffin/content/blog/`
   - Format: `YYYY-MM-DD-slug-name.md`
   - Include frontmatter: title, date, author, category, tags, image, excerpt

2. **Add to posts.json** in same directory
   - Entry needs: slug, title, excerpt, category, image, publishDate, readTime, author
   - Add new category to `categories` array if needed

3. **Add to articleContent** in `/root/projects/waitingforamacguffin/app/blog/[slug]/page.tsx`
   ```typescript
   "slug-name": loadMarkdownArticle("blog/YYYY-MM-DD-slug-name.md"),
   ```

4. **Commit & push** → Vercel auto-deploys

⚠️ If step 3 is missed, article page shows metadata but NO BODY CONTENT.

## Key Context
- Email script at `/root/.openclaw/workspace/scripts/check_email.py` (no cron active)
- Rule: Email for receiving, Telegram for talking to Mukut. Never reply by email unless asked.
- VPS git: `user.email = hello@sonderspot.com`, `user.name = Mukut`
- waitingforamacguffin path: `/root/projects/waitingforamacguffin`

## ECS Writing Skill Created
- Location: `/root/.openclaw/workspace/skills/ecs-writing/SKILL.md`
- Guide at: `/root/projects/waitingforamacguffin/ECS_LONG_FORM_COPYWRITING_GUIDE.md`
- **Rule**: Spawn sub-agent with `thinking: "high"` for all copywriting tasks
- Applies to: blog posts, articles, emails, investor comms, any external content

## Lessons Learned
- Git author must match what deployment expects (Vercel ignored wrong-author commits)
- Force push after rebase is fine for solo repos
- Claude Code with `--dangerously-skip-permissions` + project settings = smooth automation
- For copywriting: spawn sub-agent with extended thinking, don't write inline
